{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run main.nim",
      "command": "nim",
      "args": ["c", "-r", "../demo.nim"],
      "options": {
        "cwd": "${workspaceRoot}/binaries"
      },
      "type": "shell",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}